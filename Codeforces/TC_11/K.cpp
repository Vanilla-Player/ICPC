
#include <bits/stdc++.h> 
#define fast_io ios_base::sync_with_stdio(false);cin.tie(NULL);cout.tie(NULL);
using namespace std; 
#define ll long long
#define MAX_N 200050
string s;
long long n;
vector<bool > dp(MAX_N,false);


int main()
{
    fast_io;

    int dist = 0;
    cin >> n;
    vector<int> tier_on_house(2*n);

    for (int i = 0; i < 2*n; i++)
    {
        int tier;
        cin >> tier;
        tier_on_house[i] = tier;
    }

    

    for (int i = 0; i < count; i++)
    {
        /* code */
    }
    
    

   return 0;
}