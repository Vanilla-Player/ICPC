#include <bits/stdc++.h>

using namespace std;

int main(){

    ios::sync_with_stdio(0);
    cin.tie(0);

    int n,m;
    int f,f1;
    cin >> n;
    cin >> m;
    for (int i = 0; i < m; i++)
    {
        cin >> f >> f1; 
        /* code */
    }

    for (int i = 0; i < n; i++)
    {
        if(i%2 == 0){
            cout << '0';
        }else{
            cout << '1';
        }
    }
    cout << endl;

}